# Feature Specification: Pipedrive Webhooks Integration

**Feature Branch**: `013-pipedrive-webhooks`  
**Created**: 2025-11-18  
**Status**: ✅ Implemented  
**Input**: User description: "Настроить Pipedrive webhooks для точечных обновлений вместо polling всей базы"

## User Scenarios & Testing *(mandatory)*

### User Story 1 - Обработка изменений invoice_type через webhook (Priority: P1)

Финансовый координатор изменяет поле `invoice_type` в сделке Pipedrive, и система мгновенно обрабатывает изменение: создает инвойс в wFirma или Stripe Checkout Session без необходимости ждать следующего hourly polling.

**Why this priority**: Мгновенная обработка изменений критична для оперативности бизнес-процессов и снижает нагрузку на Pipedrive API.

**Independent Test**: Изменить `invoice_type` в тестовой сделке, проверить, что webhook приходит и обрабатывается в течение нескольких секунд.

**Acceptance Scenarios**:

1. **Given** сделка с `invoice_type = null`, **When** пользователь устанавливает `invoice_type = 75` (Stripe), **Then** система создает Stripe Checkout Session в течение 5 секунд.
2. **Given** сделка с `invoice_type = null`, **When** пользователь устанавливает `invoice_type = 70` (Proforma), **Then** система создает инвойс в wFirma в течение 10 секунд.
3. **Given** сделка с активным инвойсом, **When** пользователь устанавливает `invoice_type = 74` (Delete), **Then** система удаляет инвойс в течение 5 секунд.

---

### User Story 2 - Обработка рефандов через webhook (Priority: P1)

Операционный менеджер изменяет статус сделки на "lost" с reason "Refund", и система автоматически обрабатывает рефанды для всех связанных Stripe платежей без необходимости ждать hourly polling.

**Why this priority**: Рефанды - редкий, но критичный кейс, требующий мгновенной обработки для финансовой отчетности.

**Independent Test**: Изменить статус сделки на "lost" с reason "Refund", проверить, что рефанды обработаны в течение нескольких секунд.

**Acceptance Scenarios**:

1. **Given** сделка со статусом "open" и активными Stripe платежами, **When** пользователь изменяет статус на "lost" с reason "Refund", **Then** система обрабатывает рефанды для всех платежей в течение 10 секунд.
2. **Given** webhook приходит от workflow automation с `Deal_status = "lost"` и `Deal_lost_reason = "Refund"`, **When** система получает событие, **Then** рефанды обрабатываются корректно.

---

### User Story 3 - История webhook событий для отладки (Priority: P2)

Разработчик или оператор может просмотреть историю последних webhook событий для отладки и мониторинга работы системы.

**Why this priority**: Прозрачность и возможность отладки критичны для поддержки системы в production.

**Independent Test**: Отправить тестовый webhook, проверить, что событие появилось в истории и доступно через API.

**Acceptance Scenarios**:

1. **Given** система получает webhook событие, **When** оператор запрашивает `/api/webhooks/pipedrive/history`, **Then** событие отображается в списке с timestamp, dealId и preview данных.
2. **Given** оператор хочет увидеть полное тело события, **When** он запрашивает `/api/webhooks/pipedrive/history/0`, **Then** возвращается полное тело webhook события.

---

## Requirements *(mandatory)*

### Functional Requirements

- **FR-001**: Система MUST обрабатывать webhook события от Pipedrive в формате `deal.updated` и workflow automation формате.
- **FR-002**: Webhook endpoint `/api/webhooks/pipedrive` MUST быть доступен без авторизации для Pipedrive.
- **FR-003**: Система MUST обрабатывать изменения `invoice_type` и создавать соответствующие инвойсы или Checkout Sessions.
- **FR-004**: Система MUST обрабатывать изменения статуса на "lost" с reason "Refund" и создавать рефанды.
- **FR-005**: Система MUST сохранять последние 50 webhook событий в памяти для отладки.
- **FR-006**: Система MUST предоставлять API endpoint `/api/webhooks/pipedrive/history` для просмотра истории событий.
- **FR-007**: Polling MUST оставаться как fallback механизм для обработки пропущенных событий.
- **FR-008**: Рефанды (`processLostDealRefunds`) MUST обрабатываться только через webhooks, не через polling.

### Key Entities *(include if feature involves data)*

- **WebhookEvent**: Запись о полученном webhook событии (timestamp, event type, dealId, body, bodyKeys).
- **WebhookHistory**: In-memory хранилище последних 50 событий для отладки.

## Success Criteria *(mandatory)*

### Measurable Outcomes

- **SC-001**: 100% изменений `invoice_type` обрабатываются через webhooks в течение 10 секунд.
- **SC-002**: 100% рефандов обрабатываются через webhooks в течение 10 секунд.
- **SC-003**: История webhook событий доступна через API без ошибок.
- **SC-004**: Polling используется только как fallback (менее 5% обработок через polling).

## Implementation Status

### ✅ Completed

1. **Webhook endpoint** - `/api/webhooks/pipedrive` реализован и протестирован
2. **Обработка invoice_type** - создание инвойсов и Stripe Checkout Sessions через webhooks
3. **Обработка рефандов** - обработка статуса "lost" с reason "Refund" через webhooks
4. **История событий** - endpoint `/api/webhooks/pipedrive/history` для просмотра последних событий
5. **Удаление polling для рефандов** - `processLostDealRefunds()` удален из hourly polling
6. **Поддержка workflow automation** - обработка расширенного формата данных от Pipedrive автоматизаций

### 📝 Documentation

- `docs/pipedrive-webhook-setup.md` - инструкция по настройке webhooks в Pipedrive
- `docs/pipedrive-workflow-webhook-fields.md` - рекомендуемые поля для workflow automation
- `docs/pipedrive-automations-setup.md` - настройка автоматизаций в Pipedrive
- `docs/testing-webhooks-without-tokens.md` - тестирование webhooks без API токенов

### 🧪 Testing

- Тестовые скрипты: `scripts/test-pipedrive-webhook.js`, `scripts/test-pipedrive-webhook-mock.js`
- Webhook события успешно обрабатываются на продакшене
- История событий работает корректно

## Assumptions & Dependencies *(optional)*

- Pipedrive webhook настроен на отправку событий на `https://invoices.comoon.io/api/webhooks/pipedrive`
- Webhook поддерживает два формата: стандартный Pipedrive и workflow automation
- Polling остается как fallback для обработки пропущенных событий
- Rate limit Pipedrive API может ограничивать обработку webhook событий (требует восстановления лимита)


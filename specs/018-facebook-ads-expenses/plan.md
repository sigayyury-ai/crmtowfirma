# Implementation Plan: Facebook Ads Expenses Integration

**Branch**: `018-facebook-ads-expenses` | **Date**: 2025-01-XX | **Spec**: [`/specs/018-facebook-ads-expenses/spec.md`](spec.md)

## Summary

Реализация системы импорта и обработки расходов Facebook Ads из CSV отчетов с автоматическим маппингом рекламных кампаний на продукты и накопительным учетом расходов для финальных отчетов по ват-марже.

## Technical Context

### Existing Components

- **Payment Processing**: `src/services/payments/bankStatementParser.js` - парсер банковских CSV (не подходит, т.к. ориентирован на банковские транзакции)
- **Product Reports**: `src/services/vatMargin/productReportService.js` - сервис отчетов по продуктам
- **Payment-Product Links**: `src/services/payments/paymentProductLinkService.js` - сервис связи платежей с продуктами
- **Frontend**: `frontend/vat-margin-product.html/js` - интерфейс отчета по продуктам

### Why Bank Processor Doesn't Fit

Процессор банковских файлов (`bankStatementParser.js`) не подходит, потому что:
1. Ориентирован на банковские транзакции с определенной структурой (дата, описание, сумма, валюта)
2. Не поддерживает накопительную логику (каждый месяц расходы увеличиваются)
3. Не имеет системы маппинга кампаний на продукты
4. Структура CSV Facebook Ads отличается от банковских выписок

## Implementation Phases

### Phase 1: Database Schema & Core Models

**Tasks:**
1. Создать миграцию для таблиц `facebook_ads_campaign_mappings`, `facebook_ads_expenses`, `facebook_ads_import_batches`
2. Создать модель `FacebookAdsCampaignMapping` (если нужна)
3. Создать модель `FacebookAdsExpense` (если нужна)

**Files:**
- `scripts/migrations/020_create_facebook_ads_tables.sql`
- `src/services/facebookAds/facebookAdsMappingService.js` (опционально)
- `src/services/facebookAds/facebookAdsExpenseService.js` (опционально)

**Estimate**: 4-6 hours

### Phase 2: CSV Parser

**Tasks:**
1. Создать парсер CSV отчетов Facebook Ads
2. Извлечение названий кампаний и сумм расходов
3. Определение месяца расходов из даты/периода
4. Нормализация названий кампаний

**Files:**
- `src/services/facebookAds/facebookAdsCsvParser.js`

**Estimate**: 6-8 hours

**CSV Format Research:**
- Нужно получить пример CSV файла от пользователя
- Определить точные названия колонок
- Определить формат дат/периодов

### Phase 3: Mapping Service

**Tasks:**
1. Создать сервис для управления маппингом кампаний на продукты
2. CRUD операции для маппингов
3. Автоматическое предложение маппинга на основе нормализованных названий
4. Поиск существующего маппинга

**Files:**
- `src/services/facebookAds/facebookAdsMappingService.js`
- `src/routes/api.js` - добавить эндпоинты для маппингов

**Estimate**: 6-8 hours

### Phase 4: Import Service

**Tasks:**
1. Создать сервис импорта CSV файлов
2. Интеграция парсера, маппинга и создания записей
3. Расчет дельты расходов между месяцами
4. Обработка остановленных кампаний
5. Предотвращение дубликатов

**Files:**
- `src/services/facebookAds/facebookAdsImportService.js`
- `src/routes/api.js` - добавить эндпоинт `POST /api/facebook-ads/import`

**Estimate**: 8-10 hours

### Phase 5: Integration with Product Reports

**Tasks:**
1. Модифицировать `ProductReportService` для загрузки расходов Facebook Ads
2. Интеграция с `loadLinkedPayments` или создание отдельного метода
3. Отображение расходов в разделе "Связанные платежи"

**Files:**
- `src/services/vatMargin/productReportService.js`
- `frontend/vat-margin-product.js` - обновить отображение

**Estimate**: 4-6 hours

### Phase 6: Frontend - Import Interface

**Tasks:**
1. Добавить подвкладку "📊 Facebook Ads" в `vat-margin.html` (в разделе "Платежи")
2. Создать интерфейс загрузки CSV файлов внутри подвкладки
3. Прогресс-бар при обработке
4. Отображение результатов импорта
5. Обработка ошибок
6. Обновить JavaScript для обработки новой подвкладки

**Files:**
- `frontend/vat-margin.html` - добавить подвкладку и секцию
- `frontend/vat-margin-script.js` - добавить обработку подвкладки `facebook-ads`
- `frontend/facebook-ads-script.js` (новый файл для логики Facebook Ads)

**Estimate**: 6-8 hours

### Phase 7: Frontend - Mapping Management

**Tasks:**
1. Создать интерфейс управления маппингом внутри подвкладки Facebook Ads
2. Внутренние вкладки: "Импорт CSV", "Маппинг кампаний", "Статистика"
3. Таблица со списком маппингов
4. Поиск и фильтрация
5. Создание/редактирование/удаление маппингов
6. Список unmapped кампаний

**Files:**
- `frontend/vat-margin.html` - добавить контент подвкладки с внутренними вкладками
- `frontend/facebook-ads-script.js` - логика управления маппингом

**Estimate**: 8-10 hours

### Phase 8: Testing & Documentation

**Tasks:**
1. Написать unit тесты для парсера
2. Написать integration тесты для импорта
3. Обновить документацию API
4. Создать инструкцию для пользователей

**Files:**
- `tests/unit/facebookAdsCsvParser.test.js`
- `tests/integration/facebookAdsImport.test.js`
- `docs/api-reference.md` - обновить

**Estimate**: 6-8 hours

## Total Estimate

**48-64 hours** (6-8 рабочих дней)

## Dependencies

- Supabase client настроен
- Таблица `products` существует
- Таблица `payment_product_links` существует (для интеграции)
- Пример CSV файла Facebook Ads для тестирования

## Risks & Mitigation

### Risk 1: Формат CSV Facebook Ads может отличаться от ожидаемого
**Mitigation**: 
- Запросить пример CSV файла у пользователя перед началом разработки
- Сделать парсер гибким, поддерживающим несколько форматов
- Добавить валидацию с понятными сообщениями об ошибках

### Risk 2: Накопительная логика может быть сложной
**Mitigation**:
- Начать с простой реализации (хранение расходов по месяцам)
- Добавить флаг `is_campaign_active` для отслеживания остановленных кампаний
- Протестировать на реальных данных

### Risk 3: Производительность при больших объемах данных
**Mitigation**:
- Использовать батчинг при импорте
- Добавить индексы на ключевые поля
- Оптимизировать запросы к базе данных

## Success Criteria

1. ✅ Импорт CSV файла Facebook Ads работает корректно
2. ✅ Маппинг кампаний на продукты создается и сохраняется
3. ✅ Расходы отображаются в отчете по продуктам в разделе "Связанные платежи"
4. ✅ Накопительная логика работает (расходы увеличиваются каждый месяц)
5. ✅ Остановленные кампании определяются корректно
6. ✅ Дубликаты не создаются при повторном импорте


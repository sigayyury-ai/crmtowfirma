# 018-facebook-ads-expenses

## Интеграция расходов Facebook Ads в отчеты по продуктам

### Обзор

Система для импорта и обработки расходов на рекламу из Facebook Ads (CSV отчеты) с автоматическим маппингом рекламных кампаний на продукты и накопительным учетом расходов для финальных отчетов по ват-марже.

### Бизнес-контекст

Для финальных отчетов по продуктам в системе ват-маржи нужны все расходы, включая расходы на рекламу. Facebook Ads списывает расходы по рекламным кампаниям, но эти расходы не агрегированы по продуктам. Ежемесячно доступен CSV отчет с суммами расходов по кампаниям. Каждая кампания соответствует продукту в системе. Выгрузка имеет накопительный эффект: каждый месяц расходы увеличиваются, когда рекламные кампании останавливаются - расход останавливается.

### Функциональные требования

#### FR-001: Импорт CSV отчетов Facebook Ads
**Описание:** Система ДОЛЖНА позволять загружать CSV файлы с расходами по рекламным кампаниям Facebook Ads.

**Критерии приемки:**
- Поддержка формата CSV отчетов Facebook Ads
- Парсинг названий кампаний и сумм расходов
- Валидация формата файла перед импортом
- Обработка ошибок парсинга с понятными сообщениями

#### FR-002: Маппинг кампаний на продукты
**Описание:** Система ДОЛЖНА поддерживать маппинг названий рекламных кампаний на продукты в системе.

**Критерии приемки:**
- Сохранение маппинга для старых кампаний
- Возможность создания маппинга для новых кампаний
- Интерфейс для управления маппингом (создание, редактирование, удаление)
- Автоматическое предложение маппинга на основе нормализованных названий
- Поддержка нескольких кампаний на один продукт

#### FR-003: Накопительный учет расходов
**Описание:** Система ДОЛЖНА учитывать накопительный характер расходов: CSV содержит накопительные суммы за весь период отчетности (от даты начала до даты окончания). При повторном импорте того же периода сумма должна обновляться, а не создаваться дубликат.

**Критерии приемки:**
- Хранение накопительных сумм расходов за период отчетности
- Обновление существующих записей при повторном импорте того же периода
- Обработка дубликатов названий кампаний (несколько записей с одним названием)
- Отображение накопленных расходов в отчетах
- Поддержка нескольких кампаний для одного продукта

#### FR-004: Создание записей расходов
**Описание:** Система ДОЛЖНА создавать записи расходов, связанные с продуктами через маппинг кампаний. **ВАЖНО:** Расходы Facebook Ads НЕ должны попадать в PNL отчет, так как туда попадают только реальные расходы из банковских выгрузок.

**Критерии приемки:**
- Создание записей в таблице `facebook_ads_expenses` (основное хранилище)
- Создание записей в таблице `payments` с `direction='out'` и `source='facebook_ads'` ТОЛЬКО для отображения в отчете по продуктам
- Связь расходов с продуктами через `payment_product_links`
- Сохранение метаданных: название кампании, период отчетности, сумма
- Автоматическое определение валюты (обычно PLN для Facebook Ads)
- **Исключение из PNL:** PNL отчет должен исключать расходы с `source='facebook_ads'` из расчетов

#### FR-005: Отображение в отчете по продуктам
**Описание:** Система ДОЛЖНА отображать расходы Facebook Ads в разделе "Связанные платежи" отчета по продуктам.

**Критерии приемки:**
- Расходы отображаются в разделе "Связанные платежи" вместе с банковскими платежами
- Группировка по типу (исходящие платежи)
- Отображение названия кампании, месяца, суммы
- Включение в расчет общей суммы расходов продукта

#### FR-006: Управление маппингом
**Описание:** Система ДОЛЖНА предоставлять отдельную страницу в UI для управления маппингом кампаний на продукты и сопоставления расходов по маркетингу.

**Критерии приемки:**
- Отдельная страница `/facebook-ads-mappings.html` для управления маппингами
- Список всех маппингов с возможностью редактирования
- Список неразмеченных кампаний (unmapped) с возможностью создания маппинга
- Поиск и фильтрация маппингов и кампаний
- Создание нового маппинга для неразмеченных кампаний
- Редактирование маппинга (изменение продукта)
- Удаление маппинга (с сохранением истории)
- Поддержка нескольких кампаний для одного продукта
- Отображение статистики: сколько кампаний размечено, сколько не размечено
- Экспорт/импорт маппингов (опционально)

#### FR-007: Обработка дубликатов
**Описание:** Система ДОЛЖНА предотвращать создание дублирующих записей при повторном импорте того же CSV файла и корректно обрабатывать дубликаты названий кампаний в одном файле.

**Критерии приемки:**
- Проверка на существующие записи по комбинации: кампания + период отчетности (дата начала + дата окончания)
- Идемпотентность импорта (повторный импорт обновляет существующие записи, а не создает дубликаты)
- Обработка дубликатов названий кампаний в одном CSV файле (несколько записей с одним названием, но разными суммами/периодами)
- Логирование пропущенных дубликатов и обновленных записей
- Использование хеша файла для предотвращения повторного импорта идентичного файла

#### FR-008: Обработка остановленных кампаний
**Описание:** Система ДОЛЖНА определять остановленные кампании (когда расходы не изменяются между импортами) и отмечать их статус.

**Критерии приемки:**
- Определение остановленных кампаний по отсутствию изменения суммы расходов между импортами
- Автоматическое обновление статуса кампании (активна/остановлена)
- Отображение статуса кампании в интерфейсе управления маппингом
- Возможность ручного изменения статуса кампании

### Нефункциональные требования

#### NFR-001: Производительность
- Импорт CSV файла (до 1000 строк): < 5 секунд
- Поиск маппинга: < 100ms
- Загрузка расходов в отчет: < 1 секунды

#### NFR-002: Безопасность
- Валидация входных данных
- Защита от SQL-инъекций
- Ограничение размера загружаемых файлов (до 10MB)

#### NFR-003: Удобство использования
- Понятные сообщения об ошибках
- Прогресс-бар при импорте
- Предпросмотр данных перед импортом

### Технологические решения

#### Архитектура
- Backend: Node.js с Supabase
- Frontend: Vanilla JavaScript (интеграция в существующие интерфейсы)
- База данных: PostgreSQL через Supabase

#### Структура данных

**Таблица: `facebook_ads_campaign_mappings`**
```sql
CREATE TABLE facebook_ads_campaign_mappings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  campaign_name TEXT NOT NULL,
  campaign_name_normalized TEXT NOT NULL, -- для поиска
  product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  created_by VARCHAR(255),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
  -- Убрали UNIQUE на campaign_name_normalized, т.к. несколько кампаний могут мапиться на один продукт
  -- Но добавим UNIQUE(campaign_name_normalized, product_id) если нужно предотвратить дубликаты
);
CREATE INDEX idx_facebook_ads_mappings_campaign ON facebook_ads_campaign_mappings(campaign_name_normalized);
CREATE INDEX idx_facebook_ads_mappings_product ON facebook_ads_campaign_mappings(product_id);
```

**Таблица: `facebook_ads_expenses`**
```sql
CREATE TABLE facebook_ads_expenses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  campaign_name TEXT NOT NULL,
  campaign_name_normalized TEXT NOT NULL,
  product_id BIGINT REFERENCES products(id) ON DELETE SET NULL,
  report_start_date DATE NOT NULL, -- дата начала отчетности
  report_end_date DATE NOT NULL, -- дата окончания отчетности
  amount_pln NUMERIC(12, 2) NOT NULL, -- накопительная сумма за период
  currency VARCHAR(3) DEFAULT 'PLN',
  is_campaign_active BOOLEAN DEFAULT TRUE, -- активна ли кампания
  import_batch_id UUID, -- для группировки импортов
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(campaign_name_normalized, report_start_date, report_end_date)
);
CREATE INDEX idx_facebook_ads_expenses_campaign ON facebook_ads_expenses(campaign_name_normalized);
CREATE INDEX idx_facebook_ads_expenses_product ON facebook_ads_expenses(product_id);
CREATE INDEX idx_facebook_ads_expenses_dates ON facebook_ads_expenses(report_start_date, report_end_date);
```

**Связь с существующей системой:**
- Расходы Facebook Ads хранятся в таблице `facebook_ads_expenses` (основное хранилище)
- Для отображения в отчете по продуктам создаются записи в таблице `payments` с `direction='out'` и `source='facebook_ads'`
- Связь с продуктами через `payment_product_links`
- **Исключение из PNL:** PNL отчет должен фильтровать расходы с `source='facebook_ads'` и НЕ включать их в расчеты, так как PNL использует только реальные банковские транзакции

#### API эндпоинты

**Импорт CSV:**
- `POST /api/facebook-ads/import` - загрузка и обработка CSV файла
  - Body: FormData с файлом
  - Response: `{ success: boolean, data: { processed: number, mapped: number, unmapped: number, errors: [] } }`

**Управление маппингом:**
- `GET /api/facebook-ads/mappings` - список всех маппингов
- `POST /api/facebook-ads/mappings` - создание маппинга
  - Body: `{ campaign_name: string, product_id: number }`
- `PUT /api/facebook-ads/mappings/:id` - обновление маппинга
- `DELETE /api/facebook-ads/mappings/:id` - удаление маппинга

**Расходы:**
- `GET /api/facebook-ads/expenses` - список расходов (с фильтрами)
- `GET /api/facebook-ads/expenses/unmapped` - кампании без маппинга

#### Формат CSV Facebook Ads

**Структура CSV файла:**
- Заголовок: `"Название кампании",Валюта,"Сумма затрат (PLN)","Дата начала отчетности","Дата окончания отчетности"`
- Данные: название кампании (может быть в кавычках или без), валюта (обычно PLN), сумма затрат в PLN, дата начала (YYYY-MM-DD), дата окончания (YYYY-MM-DD)
- Сумма затрат - это накопительная сумма за весь период отчетности (от даты начала до даты окончания)

**Пример структуры:**
```csv
"Название кампании",Валюта,"Сумма затрат (PLN)","Дата начала отчетности","Дата окончания отчетности"
,PLN,65572.98,2025-01-01,2025-12-31
spain-march,PLN,5336.88,2025-01-01,2025-12-31
"France 2",PLN,5145.32,2025-01-01,2025-12-31
"Camp / NY2026",PLN,3189.86,2025-01-01,2025-12-31
"Event / NY2026",PLN,1149.78,2025-01-01,2025-12-31
```

**Особенности:**
- Название кампании может быть в кавычках или без
- Могут быть дубликаты названий кампаний (несколько записей с одним названием, но разными суммами/периодами)
- Валюта обычно PLN, но может быть другая
- Период отчетности может быть разным для разных кампаний

### Процесс импорта

1. **Загрузка CSV файла**
   - Пользователь загружает CSV через интерфейс
   - Валидация формата файла

2. **Парсинг данных**
   - Извлечение названий кампаний и сумм
   - Определение месяца расходов
   - Нормализация названий кампаний

3. **Маппинг на продукты**
   - Поиск существующего маппинга по нормализованному названию
   - Для новых кампаний - предложение маппинга или пометка как unmapped

4. **Обработка накопительных сумм**
   - CSV содержит накопительные суммы за весь период отчетности
   - Для каждой кампании: проверка существующей записи по комбинации (название + период)
   - Если запись существует - обновление суммы
   - Если запись не существует - создание новой записи
   - Если сумма не изменилась между импортами - кампания может быть остановлена

5. **Создание/обновление записей**
   - Создание или обновление записей в `facebook_ads_expenses` (основное хранилище)
   - Создание записей в `payments` с `source='facebook_ads'` для отображения в отчете по продуктам
   - Создание связей в `payment_product_links` для отображения в отчетах
   - **Важно:** Записи в `payments` с `source='facebook_ads'` должны быть помечены так, чтобы PNL отчет их исключал

6. **Отображение в отчетах**
   - Расходы автоматически появляются в разделе "Связанные платежи" отчета по продуктам (VAT Margin)
   - Включение в расчет общей суммы расходов продукта в отчете по продуктам
   - **Исключение из PNL:** Расходы Facebook Ads НЕ должны попадать в PNL отчет, так как PNL использует только реальные банковские транзакции

### Интерфейсные требования

#### UI-001: Импорт CSV
- Вкладка "Импорт CSV" в подвкладке "Facebook Ads" раздела "Платежи"
- Форма загрузки с drag & drop или выбором файла
- Прогресс-бар при обработке
- Отображение результатов импорта:
  - Общее количество обработанных строк
  - Количество размеченных кампаний (с маппингом)
  - Количество неразмеченных кампаний (требуют маппинга)
  - Количество обновленных записей
  - Количество созданных записей
  - Список ошибок (если есть)
- История импортов (список последних импортов с датами и результатами)

#### UI-002: Подвкладка в разделе "Платежи"
- Подвкладка "📊 Facebook Ads" в вкладке "Платежи" раздела "Отчеты" (справа от "🔍 Диагностика")
- Управление маппингами и сопоставление расходов по маркетингу встроено в существующую структуру
- Три внутренние вкладки:
  - **Импорт CSV**: загрузка CSV файлов, результаты импорта, история импортов
  - **Маппинг кампаний**: 
    - Размеченные кампании (таблица со списком всех маппингов с возможностью редактирования/удаления)
    - Неразмеченные кампании (список кампаний без маппинга с возможностью создания маппинга)
  - **Статистика**: общее количество кампаний, размеченных и неразмеченных, сумма расходов по продуктам
- Поиск и фильтрация по названию кампании и продукту
- Редактирование маппинга (изменение продукта)
- Создание маппинга для unmapped кампаний (выбор продукта из выпадающего списка)
- Отображение суммы расходов для каждой кампании

#### UI-003: Отображение в отчете по продуктам
- Расходы Facebook Ads в разделе "Связанные платежи"
- Группировка с другими исходящими платежами
- Отображение названия кампании и месяца

### Критерии готовности

#### Definition of Ready
- [ ] Спецификация утверждена
- [ ] Формат CSV Facebook Ads определен и протестирован
- [ ] Архитектурное решение согласовано
- [ ] Оценка трудозатрат выполнена

#### Definition of Done
- [ ] Все функциональные требования реализованы
- [ ] Импорт CSV работает корректно
- [ ] Маппинг кампаний на продукты работает
- [ ] Расходы отображаются в отчете по продуктам
- [ ] Автоматизированные тесты написаны
- [ ] Код прошел ревью
- [ ] Документация обновлена
- [ ] Функциональное тестирование пройдено

### Альтернативные решения

#### Вариант 1: Использование существующей таблицы payments
**Плюсы:**
- Единообразие с банковскими платежами
- Переиспользование существующей логики

**Минусы:**
- Структура payments может не подходить для накопительных расходов
- Сложнее отслеживать дельты между месяцами

#### Вариант 2: Отдельная таблица facebook_ads_expenses
**Плюсы:**
- Специализированная структура для накопительных расходов
- Легче отслеживать историю изменений

**Минусы:**
- Дублирование логики отображения
- Нужна дополнительная интеграция с payment_product_links

**Рекомендация:** Использовать вариант 2 (отдельная таблица) с интеграцией через payment_product_links для единообразия отображения.


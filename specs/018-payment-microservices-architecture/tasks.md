# Tasks: Payment Microservices Architecture - Validation Service

**Input**: Design documents from `/specs/018-payment-microservices-architecture/`
**Prerequisites**: plan.md ✅, spec.md ✅, data-model.md ✅, contracts/ ✅, quickstart.md ✅

**Focus**: User Story 8 - Validation Service (Priority P1) - Реализация валидации данных для предотвращения ошибок при создании платежных сессий

**Tests**: Ручные интеграционные тесты через скрипты и существующие эндпоинты диагностики

**Organization**: Tasks организованы по фазам для постепенной реализации валидации

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Может выполняться параллельно (разные файлы, нет зависимостей)
- **[Story]**: К какой user story относится задача (US8 для валидации)
- В описании указаны точные пути к файлам

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Инициализация структуры проекта для микросервисов

- [ ] T001 Создать директорию для микросервисов `src/services/microservices/`
- [ ] T002 [P] Создать базовый класс `BaseMicroservice` в `src/services/microservices/baseMicroservice.js`
- [ ] T003 [P] Создать директорию для миграций валидации `scripts/migrations/` (если не существует)

**Checkpoint**: Структура проекта готова для создания микросервисов

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Базовая инфраструктура, которая ДОЛЖНА быть завершена перед реализацией Validation Service

**⚠️ CRITICAL**: Реализация валидации не может начаться до завершения этой фазы

- [ ] T004 Создать миграцию для таблицы `validation_errors` в `scripts/migrations/022_create_validation_errors.sql`
- [ ] T005 Выполнить миграцию `022_create_validation_errors.sql` в Supabase
- [ ] T006 [P] Проверить создание таблицы `validation_errors` через SQL запрос
- [ ] T007 [P] Создать базовый класс `BaseMicroservice` с методами логирования и обработки ошибок в `src/services/microservices/baseMicroservice.js`

**Checkpoint**: База данных готова, базовые классы созданы - можно начинать реализацию Validation Service

---

## Phase 3: User Story 8 - Validation Service (Priority: P1) 🎯 MVP

**Goal**: Реализовать Validation Service для валидации данных перед созданием Stripe Checkout Session. Система должна проверять обязательные поля (продукт, цена, адрес, имя клиента, email, валюта), B2B-специфичные поля (Organization, Business ID), и генерировать предупреждения для отсутствующих каналов уведомлений (SendPulse ID, Telegram Chat ID) без блокировки создания сессии.

**Independent Test**: Попытаться создать сессию без обязательных полей (продукт, цена, адрес, имя), проверить что система сохранила ошибки валидации в БД с указанием всех недостающих полей, не создала сессию, но не упала с ошибкой. Исправить данные, перезапустить процесс, проверить что сессия создана успешно.

### Implementation for User Story 8

- [ ] T008 [US8] Создать `ValidationService` класс в `src/services/microservices/validationService.js` с конструктором и базовыми методами
- [ ] T009 [US8] Реализовать метод `validateSessionData(data)` в `src/services/microservices/validationService.js` с проверкой обязательных полей (deal_id, deal_status)
- [ ] T010 [US8] Добавить валидацию продукта (product) в `validateSessionData` - проверка наличия product.id и product.name
- [ ] T011 [US8] Добавить валидацию цены (amount) в `validateSessionData` - проверка что amount > 0, является числом, не превышает deal_amount
- [ ] T012 [US8] Добавить валидацию адреса (address) в `validateSessionData` - проверка наличия address.country, для PL проверка полноты адреса
- [ ] T013 [US8] Добавить валидацию имени клиента (customer_name) в `validateSessionData` - проверка что имя не пустое
- [ ] T014 [US8] Добавить валидацию email в `validateSessionData` - проверка формата email через `isValidEmail` метод
- [ ] T015 [US8] Добавить валидацию валюты (currency) в `validateSessionData` - проверка что currency в списке поддерживаемых (PLN, EUR, USD, GBP)
- [ ] T016 [US8] Добавить определение B2B сделки в `validateSessionData` - проверка customer_type, organization_id, organization
- [ ] T017 [US8] Добавить B2B валидацию Organization в `validateSessionData` - проверка что organization_id заполнен и не равен 0/null для B2B
- [ ] T018 [US8] Добавить B2B валидацию Business ID (company_tax_id) в `validateSessionData` - проверка наличия NIP/VAT для B2B
- [ ] T019 [US8] Добавить B2B валидацию company_name в `validateSessionData` - проверка наличия названия компании для B2B
- [ ] T020 [US8] Добавить валидацию каналов уведомлений (SendPulse ID, Telegram Chat ID) в `validateSessionData` - генерировать предупреждения (warnings), а не ошибки, если отсутствуют
- [ ] T021 [US8] Реализовать метод `isValidEmail(email)` в `ValidationService` для проверки формата email
- [ ] T022 [US8] Реализовать метод `saveValidationError(dealId, processType, validationResult, data, processId)` в `ValidationService` для сохранения ошибок в БД
- [ ] T023 [US8] Реализовать метод `saveValidationWarning(dealId, processType, warnings, data, processId)` в `ValidationService` для сохранения предупреждений в БД с severity='warning'
- [ ] T024 [US8] Реализовать метод `validatePaymentData(data)` в `ValidationService` для валидации данных платежа (базовая реализация)
- [ ] T025 [US8] Добавить логирование всех операций валидации через `this.log()` в `ValidationService`

**Checkpoint**: Validation Service полностью реализован и может валидировать данные сессий. Можно интегрировать с существующими сервисами.

---

## Phase 4: Integration with Existing Services

**Purpose**: Интеграция Validation Service с существующими сервисами создания сессий и диагностики

- [ ] T026 [US8] Импортировать `ValidationService` в `src/services/stripe/paymentSessionCreator.js`
- [ ] T027 [US8] Инициализировать `ValidationService` в конструкторе `PaymentSessionCreator`
- [ ] T028 [US8] Добавить вызов `validationService.validateSessionData()` в начале метода `createSession()` в `PaymentSessionCreator` ПЕРЕД созданием Stripe Checkout Session
- [ ] T029 [US8] Обработать результат валидации в `PaymentSessionCreator.createSession()` - если есть ошибки (errors), сохранить их через `saveValidationError`, вернуть ошибку без создания сессии
- [ ] T030 [US8] Обработать предупреждения (warnings) в `PaymentSessionCreator.createSession()` - если есть warnings, сохранить их через `saveValidationWarning`, логировать, но продолжить создание сессии
- [ ] T031 [US8] Создать задачу в CRM для менеджера при наличии ошибок валидации через `PipedriveClient` в `PaymentSessionCreator`
- [ ] T032 [US8] Создать задачу в CRM для менеджера при наличии предупреждений валидации (отсутствие SendPulse ID/Telegram Chat ID) в `PaymentSessionCreator`
- [ ] T033 [US8] Подготовить данные для валидации в `PaymentSessionCreator.createSession()` - собрать все необходимые поля (deal, product, address, customer_name, email, currency, amount) из deal объекта
- [ ] T034 [US8] Определить B2B/B2C тип клиента в `PaymentSessionCreator.createSession()` для передачи в валидацию (проверка organization_id, invoice_type)
- [ ] T035 [US8] Извлечь SendPulse ID из кастомных полей Person в `PaymentSessionCreator.createSession()` для передачи в валидацию (поле `ff1aa263ac9f0e54e2ae7bec6d7215d027bf1b8c`)
- [ ] T036 [US8] Извлечь Telegram Chat ID из кастомных полей Person/Deal в `PaymentSessionCreator.createSession()` для передачи в валидацию
- [ ] T037 [US8] Интегрировать валидацию в `src/services/dealDiagnosticsService.js` - добавить метод `getValidationInfo(dealId)` для получения ошибок и предупреждений валидации
- [ ] T038 [US8] Добавить валидационные ошибки и предупреждения в `getDealDiagnostics()` в `DealDiagnosticsService` - включить в секцию `issues` с severity
- [ ] T039 [US8] Добавить рекомендации по исправлению ошибок валидации в `analyzeIssues()` в `DealDiagnosticsService` - генерировать рекомендации на основе missing_fields и invalid_fields
- [ ] T040 [US8] Добавить действие "Перезапустить создание сессии" в диагностику для случаев с resolved ошибками валидации в `DealDiagnosticsService`

**Checkpoint**: Validation Service интегрирован с PaymentSessionCreator и DealDiagnosticsService. Валидация срабатывает при создании сессий, ошибки сохраняются в БД и отображаются в диагностике.

---

## Phase 5: Testing & Validation

**Purpose**: Тестирование валидации через ручные интеграционные тесты

- [ ] T041 [US8] Создать тестовый скрипт `scripts/test-validation-service.js` для ручного тестирования валидации
- [ ] T042 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии без продукта - проверить что ошибка сохранена, сессия не создана
- [ ] T043 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии без цены - проверить что ошибка сохранена, сессия не создана
- [ ] T044 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии без адреса - проверить что ошибка сохранена, сессия не создана
- [ ] T045 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии без имени клиента - проверить что ошибка сохранена, сессия не создана
- [ ] T046 [US8] Добавить тест-кейс в тестовый скрипт: создание B2B сессии без Organization - проверить что ошибка сохранена, сессия не создана
- [ ] T047 [US8] Добавить тест-кейс в тестовый скрипт: создание B2B сессии без Business ID - проверить что ошибка сохранена, сессия не создана
- [ ] T048 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии без SendPulse ID и Telegram Chat ID - проверить что предупреждение сохранено, сессия создана успешно
- [ ] T049 [US8] Добавить тест-кейс в тестовый скрипт: создание сессии с корректными данными - проверить что валидация прошла, сессия создана
- [ ] T050 [US8] Проверить через диагностический эндпоинт что ошибки валидации отображаются в `getDealDiagnostics(dealId)` для сделки с ошибками
- [ ] T051 [US8] Проверить через диагностический эндпоинт что предупреждения валидации отображаются в `getDealDiagnostics(dealId)` для сделки с предупреждениями

**Checkpoint**: Все тест-кейсы пройдены, валидация работает корректно для всех сценариев.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Финальная полировка, документация и оптимизация

- [ ] T052 [P] Обновить документацию API в `docs/api-reference.md` - добавить информацию о валидации в эндпоинты создания сессий
- [ ] T053 [P] Добавить примеры использования Validation Service в `specs/018-payment-microservices-architecture/quickstart.md` (если еще не добавлено)
- [ ] T054 [P] Добавить логирование correlation ID для всех операций валидации для трассировки
- [ ] T055 Оптимизировать запросы к БД в `saveValidationError` и `saveValidationWarning` - использовать batch insert если нужно
- [ ] T056 Добавить индексы на часто используемые поля в таблице `validation_errors` если их нет (deal_id, status, severity, created_at)
- [ ] T057 Проверить что все ошибки валидации логируются с правильным уровнем (error для ошибок, warn для предупреждений)
- [ ] T058 [P] Обновить `specs/018-payment-microservices-architecture/plan.md` - отметить Phase 1 (Validation Service) как завершенную

**Checkpoint**: Валидация полностью реализована, протестирована и задокументирована.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: Нет зависимостей - можно начинать сразу
- **Foundational (Phase 2)**: Зависит от Setup - БЛОКИРУЕТ все user stories
- **User Story 8 (Phase 3)**: Зависит от Foundational - можно начинать после Phase 2
- **Integration (Phase 4)**: Зависит от User Story 8 - требует завершенного ValidationService
- **Testing (Phase 5)**: Зависит от Integration - требует интегрированного сервиса
- **Polish (Phase 6)**: Зависит от Testing - финальная полировка после тестирования

### Task Dependencies Within User Story 8

- **T008-T010**: Базовые проверки (deal_id, deal_status, product) - можно делать последовательно
- **T011-T015**: Валидация обязательных полей (amount, address, customer_name, email, currency) - можно делать параллельно [P]
- **T016-T019**: B2B валидация - зависит от T016 (определение B2B), затем T017-T019 параллельно [P]
- **T020**: Валидация каналов уведомлений - независима, можно параллельно [P]
- **T021**: Метод isValidEmail - независим, можно параллельно [P]
- **T022-T023**: Методы сохранения - независимы друг от друга, можно параллельно [P]
- **T024**: validatePaymentData - независим, можно параллельно [P]
- **T025**: Логирование - зависит от всех предыдущих методов

### Integration Dependencies

- **T026-T036**: Интеграция с PaymentSessionCreator - последовательная, каждый шаг зависит от предыдущего
- **T037-T040**: Интеграция с DealDiagnosticsService - можно делать параллельно после T026-T036 [P]

### Parallel Opportunities

- **Phase 1**: T002 и T003 можно выполнять параллельно [P]
- **Phase 2**: T006 и T007 можно выполнять параллельно [P]
- **Phase 3**: 
  - T011-T015 (валидация обязательных полей) можно делать параллельно [P]
  - T017-T019 (B2B валидация) можно делать параллельно [P]
  - T020, T021, T022, T023, T024 можно делать параллельно [P]
- **Phase 4**: T037-T040 (интеграция с DealDiagnosticsService) можно делать параллельно [P]
- **Phase 5**: T042-T048 (тест-кейсы) можно писать параллельно [P]
- **Phase 6**: T052, T053, T054, T057 можно делать параллельно [P]

---

## Parallel Example: User Story 8 Implementation

```bash
# Параллельная реализация валидации обязательных полей:
Task: "Добавить валидацию цены (amount) в validateSessionData"
Task: "Добавить валидацию адреса (address) в validateSessionData"
Task: "Добавить валидацию имени клиента (customer_name) в validateSessionData"
Task: "Добавить валидацию email в validateSessionData"
Task: "Добавить валидацию валюты (currency) в validateSessionData"

# Параллельная реализация методов сохранения:
Task: "Реализовать метод saveValidationError"
Task: "Реализовать метод saveValidationWarning"
Task: "Реализовать метод validatePaymentData"
Task: "Реализовать метод isValidEmail"
```

---

## Implementation Strategy

### MVP First (Validation Service Only)

1. ✅ Complete Phase 1: Setup (структура проекта)
2. ✅ Complete Phase 2: Foundational (миграция БД, базовые классы)
3. ✅ Complete Phase 3: User Story 8 (Validation Service)
4. ✅ Complete Phase 4: Integration (интеграция с PaymentSessionCreator и DealDiagnosticsService)
5. ✅ Complete Phase 5: Testing (ручные интеграционные тесты)
6. ✅ Complete Phase 6: Polish (документация, оптимизация)

### Incremental Delivery

1. **Week 1**: Setup + Foundational → База готова
2. **Week 2**: Validation Service (Phase 3) → Валидация работает изолированно
3. **Week 3**: Integration (Phase 4) → Валидация интегрирована в создание сессий
4. **Week 4**: Testing + Polish (Phase 5-6) → Валидация протестирована и задокументирована

### Expected Impact

После реализации Validation Service:
- ✅ Предотвращение ошибок при создании сессий с неполными данными
- ✅ Понятные сообщения об ошибках для менеджеров
- ✅ Автоматическое создание задач в CRM при ошибках валидации
- ✅ Отображение проблем валидации в Deal Diagnostics Service
- ✅ Защита от создания сессий без обязательных полей (продукт, цена, адрес, имя)
- ✅ B2B валидация для предотвращения ошибок с инвойсами
- ✅ Предупреждения о недостающих каналах уведомлений без блокировки процесса

---

## Notes

- [P] задачи = разные файлы, нет зависимостей
- [US8] метка связывает задачу с User Story 8 для трассируемости
- Каждая фаза должна быть независимо завершаемой и тестируемой
- Проверять тесты после каждой фазы
- Коммитить после каждой задачи или логической группы
- Останавливаться на любом checkpoint для валидации фазы независимо
- Избегать: расплывчатых задач, конфликтов в одном файле, зависимостей между фазами которые нарушают независимость

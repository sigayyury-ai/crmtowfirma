/**
 * Конфигурация банковских счетов для интеграции Pipedrive-wFirma
 * 
 * Этот файл содержит настройки для сопоставления валют из Pipedrive CRM
 * с банковскими счетами в системе wFirma.
 * 
 * Для изменения настроек:
 * 1. Обновите названия счетов в поле 'name' согласно данным из wFirma
 * 2. При необходимости добавьте новые валюты
 * 3. Fallback значения используются если счет не найден по названию
 */

module.exports = {
  // Конфигурация банковских счетов по валютам
  BANK_ACCOUNT_CONFIG: {
    PLN: {
      name: 'M bank zl', // Название счета в wFirma
      fallback: 'M bank zl', // Fallback если не найдем по названию
      description: 'Основной счет в польских злотых'
    },
    EUR: {
      name: 'M Bank EUR', // Название счета в wFirma
      fallback: 'M Bank EUR', // Fallback если не найдем по названию
      description: 'Счет в евро для международных операций'
    },
    USD: {
      name: 'USD', // Название счета в wFirma
      fallback: 'USD Account', // Fallback если не найдем по названию
      description: 'Счет в долларах США'
    }
  },

  // Поддерживаемые валюты
  SUPPORTED_CURRENCIES: ['PLN', 'EUR', 'USD'],

  // Настройки кэширования банковских счетов
  CACHE_SETTINGS: {
    TTL: 3600, // Время жизни кэша в секундах (1 час)
    CHECK_PERIOD: 600 // Период проверки кэша в секундах (10 минут)
  },

  // Настройки поиска счетов
  SEARCH_SETTINGS: {
    // Искать по точному названию
    EXACT_MATCH: true,
    // Искать по частичному совпадению (первое слово)
    PARTIAL_MATCH: true,
    // Искать по валюте если не найдено по названию
    FALLBACK_TO_CURRENCY: true
  }
};

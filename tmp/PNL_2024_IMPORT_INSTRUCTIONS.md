# Инструкция по импорту PNL данных за 2024 год

## Обзор

Созданы скрипты для импорта данных PNL из Excel файла `tmp/P&L  2.xlsx` в систему статистики за 2024 год.

## Структура данных в Excel

- **Лист**: "2024"
- **Строка 1**: Курсы валют (EUR) по месяцам
- **Строка 2**: Заголовки месяцев (Февраль - Декабрь)
- **Строки 3+**: Категории расходов с суммами в EUR по месяцам

## Категории в Excel

### Расходы (expenses):
- Tools
- Sendpulse, Mailchimp, Pipedrive, Make, Music for video, Linkedin helper, Google admin
- Works
- Other
- Cost of house
- Food
- Transfer
- Referal programm
- Paid ads
- ZUS
- Бухгалтерия
- Tax / PIT
- Tax Vat
- Stripe FEE
- Возвраты по ВАТ
- Возвраты клиентам

### Доходы (revenue) - пропускаются при импорте:
- Revenue
- Stripe transaction
- Paid by cash
- Revolut
- Prepaid at client
- На вывод
- Пользование деньгами
- Доход/Убыток
- Balance
- ROI

## Маппинг категорий

Категории из Excel маппятся на категории в базе данных:

| Excel категория | База данных категория | ID |
|----------------|----------------------|-----|
| Tools | Tools | 33 |
| Sendpulse, Mailchimp, Pipedrive, Make, Music for video, Linkedin helper, Google admin, Paid ads | Marketing & Advertising | 20 |
| Works, Бухгалтерия | Услуги/Работы | 29 |
| Other | Other | 37 |
| Cost of house | Аренда домов | 35 |
| Food | Продукты и бытовые вещи | 44 |
| Transfer | Логистика | 43 |
| Referal programm | Referal programm | 41 |
| ZUS | ЗУС | 40 |
| Tax / PIT | Налоги | 38 |
| Tax Vat | ВАТ | 39 |
| Stripe FEE | Bank Fees | 21 |
| Возвраты по ВАТ, Возвраты клиентам | Возвраты клиентам | 45 |

## Варианты импорта

### Вариант 1: Прямой импорт в базу (рекомендуется) ⭐

**Используйте `importPnl2024DirectToDb.js`** - импортирует напрямую в базу данных, **без необходимости менять `management_type`**:

- ✅ Импортирует напрямую в таблицу `pnl_manual_entries`
- ✅ **Не требует изменения `management_type` категорий**
- ✅ Автоматически создает резервную копию перед импортом
- ✅ Валидирует данные перед импортом
- ✅ Позволяет откатить изменения при необходимости

### Вариант 2: Импорт через сервис (требует изменение management_type)

**Используйте `importPnl2024FromExcelSafe.js`** - использует сервис, который требует `management_type='manual'`:

- ⚠️ Требует изменения `management_type` категорий на `manual` перед импортом
- ✅ Автоматически создает резервную копию перед импортом
- ✅ Валидирует данные перед импортом
- ✅ Позволяет откатить изменения при необходимости

**Рекомендация**: Используйте **Вариант 1** (`importPnl2024DirectToDb.js`), так как он не требует изменения `management_type` и импортирует напрямую в базу.

## Вариант 1: Прямой импорт в базу (рекомендуется)

### Шаги импорта

#### 1. Анализ структуры Excel файла

```bash
node scripts/analyzePnl2024Excel.js
```

Этот скрипт покажет:
- Структуру Excel файла
- Все категории в Excel
- Категории в базе данных

#### 2. Предварительный просмотр импорта (dry-run)

```bash
node scripts/importPnl2024DirectToDb.js --dry-run
```

Этот скрипт покажет:
- Сколько записей будет импортировано
- Какие категории обработаны
- Какие категории не маппятся
- Суммы по категориям

#### 3. Прямой импорт в базу данных

```bash
node scripts/importPnl2024DirectToDb.js
```

**Что делает скрипт:**
1. ✅ Автоматически создает резервную копию всех существующих записей за 2024 год
2. ✅ Читает Excel файл и парсит данные
3. ✅ Конвертирует суммы из EUR в PLN используя курсы из Excel
4. ✅ Импортирует напрямую в таблицу `pnl_manual_entries` (минуя проверку `management_type`)
5. ✅ Показывает детальную информацию о процессе

**Опции:**
- `--dry-run` - только предварительный просмотр, без импорта и без резервной копии
- `--force` - перезаписать существующие записи без подтверждения
- `--skip-backup` - пропустить создание резервной копии (не рекомендуется)

**Пример вывода:**
```
📊 Direct PNL Import to Database for 2024
======================================================================
Mode: IMPORT (direct to database)
⚠️  This script bypasses management_type check

💾 Creating backup...
✅ Backup created: tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
📊 Backed up 0 entries

📁 Reading Excel file: tmp/P&L  2.xlsx
...
📊 Parsing expense data...

📈 Import Summary:
----------------------------------------------------------------------
Total entries to import: 154
Categories processed: 22
...

⚠️  Ready to import 154 entries directly to database. Continue? (yes/no): yes

💾 Importing directly to database...
✅ Inserted 154 entries

💾 Backup saved at: tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
💡 To restore: node scripts/backupPnlManualEntries.js 2024 tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json

✅ Import completed successfully!
```

## Вариант 2: Импорт через сервис (требует изменение management_type)

### Шаги импорта

#### 1. Анализ структуры Excel файла

```bash
node scripts/analyzePnl2024Excel.js
```

#### 2. Настройка категорий (изменение management_type)

**⚠️ ВАЖНО**: Этот вариант требует изменения `management_type` категорий на `manual` перед импортом.

**Показать текущий маппинг:**
```bash
node scripts/setupPnl2024Categories.js show
```

**Изменить категории на manual:**
```bash
node scripts/setupPnl2024Categories.js change
```

#### 3. Предварительный просмотр импорта (dry-run)

```bash
node scripts/importPnl2024FromExcelSafe.js --dry-run
```

#### 4. Импорт через сервис

```bash
node scripts/importPnl2024FromExcelSafe.js
```

**Что делает скрипт:**
1. ✅ Автоматически создает резервную копию всех существующих записей за 2024 год
2. ✅ Валидирует данные перед импортом (проверяет категории, суммы, месяцы)
3. ✅ Показывает детальную информацию о том, что будет импортировано
4. ✅ Запрашивает подтверждение перед импортом
5. ✅ Импортирует данные с обработкой ошибок
6. ✅ Сохраняет путь к резервной копии для возможного отката

**Опции:**
- `--dry-run` - только предварительный просмотр, без импорта и без резервной копии
- `--force` - перезаписать существующие записи без подтверждения
- `--skip-backup` - пропустить создание резервной копии (не рекомендуется)

**Пример вывода:**
```
📊 Safe PNL Import for 2024
======================================================================
Mode: IMPORT (will update database)

💾 Creating backup...
✅ Backup created: tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
📊 Backed up 0 entries

📁 Reading Excel file: tmp/P&L  2.xlsx
...
✅ Validating data...

📈 Import Summary:
----------------------------------------------------------------------
Total entries to import: 150
Categories processed: 13
...

⚠️  Ready to import 150 entries. Continue? (yes/no): yes

💾 Importing entries...
✅ Imported: 150
🔄 Updated: 0
❌ Errors: 0

💾 Backup saved at: tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
💡 To restore: node scripts/backupPnlManualEntries.js 2024 tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
```

#### 4. Восстановление из резервной копии (если нужно)

Если что-то пошло не так, можно восстановить данные из резервной копии:

```bash
node scripts/backupPnlManualEntries.js 2024 tmp/pnl-backups/pnl_manual_entries_2024_2025-01-15_14-30-00.json
```

**Внимание**: Это удалит все текущие записи за 2024 год и восстановит из резервной копии.

#### 5. Ручное создание резервной копии (опционально)

Можно создать резервную копию вручную в любой момент:

```bash
node scripts/backupPnlManualEntries.js 2024
```

## Восстановление из резервной копии

Если что-то пошло не так, можно восстановить данные из резервной копии:

```bash
node scripts/backupPnlManualEntries.js 2024 tmp/pnl-backups/pnl_manual_entries_2024_YYYY-MM-DD_HH-MM-SS.json
```

**Внимание**: Это удалит все текущие записи за 2024 год и восстановит из резервной копии.

## Старый метод импорта (не рекомендуется)

Если по какой-то причине нужно использовать старый скрипт без резервного копирования:

```bash
node scripts/importPnl2024FromExcel.js --dry-run  # предварительный просмотр
node scripts/importPnl2024FromExcel.js            # импорт
```

**⚠️ Внимание**: Этот скрипт не создает резервную копию автоматически. Используйте безопасный скрипт `importPnl2024FromExcelSafe.js` вместо этого.

## Важные замечания

1. **Курсы валют**: Скрипт использует курсы EUR из первой строки Excel файла для каждого месяца.

2. **Месяцы**: Данные импортируются только за месяцы Февраль - Декабрь (январь отсутствует в Excel).

3. **Категории доходов**: Категории доходов (Revenue, Stripe transaction и т.д.) пропускаются при импорте, так как это скрипт для расходов.

4. **Существующие записи**: По умолчанию скрипт пропускает существующие записи. Используйте `--force` для перезаписи.

5. **management_type**: Категории должны иметь `management_type='manual'` для импорта ручных записей.

## Проверка импортированных данных

После импорта можно проверить данные в базе:

```sql
SELECT 
  ec.name as category_name,
  me.year,
  me.month,
  me.amount_pln,
  me.currency_breakdown
FROM pnl_manual_entries me
JOIN pnl_expense_categories ec ON me.expense_category_id = ec.id
WHERE me.entry_type = 'expense' 
  AND me.year = 2024
ORDER BY ec.name, me.month;
```

## Изменение маппинга категорий

Если нужно изменить маппинг категорий, отредактируйте объект `CATEGORY_MAPPING` в файлах:
- `scripts/importPnl2024FromExcel.js`
- `scripts/setupPnl2024Categories.js`

## Поддержка

Если возникнут проблемы:
1. Проверьте структуру Excel файла с помощью `analyzePnl2024Excel.js`
2. Убедитесь, что категории имеют `management_type='manual'`
3. Проверьте логи импорта на наличие ошибок


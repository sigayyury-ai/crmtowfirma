# Рекомендуемые поля для Pipedrive Workflow Automation Webhook

Этот документ описывает, какие данные рекомендуется отправлять в webhook из Pipedrive Workflow Automation для оптимальной работы системы.

---

## 📋 Текущая настройка

**Текущий формат:**
```json
{
  "Deal ID": "1600"
}
```

Это работает, но система делает дополнительный запрос к Pipedrive API для получения полных данных сделки.

---

## ✅ Рекомендуемые поля для отправки

### Минимальный набор (для оптимизации):

```json
{
  "Deal ID": "1600",
  "Deal stage": "18",
  "Previous deal stage": "15"
}
```

**Преимущества:**
- Понимаем текущую стадию без дополнительного запроса
- Понимаем предыдущую стадию для логики обработки
- Меньше запросов к Pipedrive API

### Оптимальный набор (рекомендуется):

```json
{
  "Deal ID": "1600",
  "Deal stage": "18",
  "Previous deal stage": "15",
  "Invoice type": "75",
  "Deal status": "open",
  "Person ID": "123",
  "Organization ID": "456"
}
```

**Преимущества:**
- Все необходимые данные в одном запросе
- Не нужно делать запрос к Pipedrive API для получения данных сделки
- Не нужно делать запросы для получения данных person/organization
- Быстрее обработка
- Меньше расход токенов API

### Полный набор (если доступно):

```json
{
  "Deal ID": "1600",
  "Deal stage": "18",
  "Previous deal stage": "15",
  "Invoice type": "75",
  "Deal status": "open",
  "Deal value": "10000",
  "Deal currency": "PLN",
  "Expected close date": "2025-12-31",
  "Person ID": "123",
  "Organization ID": "456"
}
```

**Преимущества:**
- Максимум информации для обработки
- Можно принимать решения без дополнительных запросов
- Не нужно получать данные person/organization через API
- Лучшая производительность
- Минимальный расход токенов API

---

## 🔧 Как настроить в Pipedrive Workflow Automation

### Шаг 1: Откройте действие "Send webhook request"

В вашей автоматизации найдите действие **"Send webhook request"** и нажмите на него для редактирования.

### Шаг 2: Настройте Body webhook

В разделе **"Body"** добавьте следующие поля:

#### Вариант 1: Минимальный набор

**Key:** `Deal ID`  
**Value:** `{{deal.id}}`

**Key:** `Deal stage`  
**Value:** `{{deal.stage_id}}`

**Key:** `Previous deal stage`  
**Value:** `{{deal.previous_stage_id}}` (если доступно) или оставьте пустым

#### Вариант 2: Оптимальный набор (рекомендуется)

**Key:** `Deal ID`  
**Value:** `{{deal.id}}`

**Key:** `Deal stage`  
**Value:** `{{deal.stage_id}}`

**Key:** `Previous deal stage`  
**Value:** `{{deal.previous_stage_id}}` (если доступно)

**Key:** `Invoice type`  
**Value:** `{{deal.ad67729ecfe0345287b71a3b00910e8ba5b3b496}}`  
*(Замените на ключ вашего поля invoice_type, если отличается)*

**Key:** `Deal status`  
**Value:** `{{deal.status}}`

**Key:** `Person ID`  
**Value:** `{{deal.person_id}}` или `{{deal.person_id.value}}` (в зависимости от формата)  
**Примечание:** Если `person_id` в Pipedrive это объект, используйте `{{deal.person_id.value}}`, если число - `{{deal.person_id}}`

**Key:** `Organization ID`  
**Value:** `{{deal.org_id}}` или `{{deal.organization_id}}` или `{{deal.org_id.value}}` (в зависимости от формата)  
**Примечание:** Если `org_id` в Pipedrive это объект, используйте `{{deal.org_id.value}}`, если число - `{{deal.org_id}}`

#### Вариант 3: Полный набор

Добавьте все поля из варианта 2, плюс:

**Key:** `Deal value`  
**Value:** `{{deal.value}}`

**Key:** `Deal currency`  
**Value:** `{{deal.currency}}`

**Key:** `Expected close date`  
**Value:** `{{deal.expected_close_date}}`

---

## 📝 Пример настройки Body в Pipedrive

В интерфейсе Pipedrive это будет выглядеть так (на основе вашей настройки):

```
Body:
  Key: Deal_id          Value: {{deal.id}}
  Key: Contact id       Value: {{deal.person_id}} или {{deal.person_id.value}}
  Key: Organisation_id  Value: {{deal.org_id}} или {{deal.organization_id}}
  Key: Deal_stage       Value: {{deal.stage_id}}
  Key: Deal_status       Value: {{deal.status}}
  Key: Deal_close_date   Value: {{deal.expected_close_date}}
  Key: Deal_lost_reason  Value: {{deal.lost_reason}}
  Key: Invoice           Value: {{deal.ad67729ecfe0345287b71a3b00910e8ba5b3b496}}
  Key: Deal_value        Value: {{deal.value}}
  Key: Deal_currency     Value: {{deal.currency}}
```

**Примечание:** 
- Используйте точные названия ключей, как в вашей настройке (с подчеркиваниями)
- Значения берутся из переменных Pipedrive (с иконкой `1`)
- Система автоматически распознает все форматы названий полей

---

## 🔍 Как найти ключ поля invoice_type

Если вы не знаете точный ключ поля `invoice_type`:

1. Откройте любую сделку в Pipedrive
2. Откройте Developer Tools в браузере (F12)
3. Найдите поле Invoice type в форме
4. Посмотрите атрибут `name` или `data-field-key` в HTML
5. Или используйте значение из `.env`: `PIPEDRIVE_INVOICE_TYPE_FIELD_KEY`

По умолчанию используется: `ad67729ecfe0345287b71a3b00910e8ba5b3b496`

---

## 💡 Преимущества отправки дополнительных данных

### Текущая ситуация (только Deal ID):
1. Webhook получает Deal ID
2. Система делает запрос к Pipedrive API для получения данных сделки (`getDeal()`)
3. Если нужны данные person → еще один запрос (`getPerson()`)
4. Если нужны данные organization → еще один запрос (`getOrganization()`)
5. Обрабатывает данные
6. **Расход:** 1-3 дополнительных запроса = 2-6 токенов на webhook

### С дополнительными данными (включая Person ID и Organization ID):
1. Webhook получает все необходимые данные (deal + person_id + organization_id)
2. Система обрабатывает данные сразу
3. **Расход:** 0 дополнительных запросов = экономия 2-6 токенов на каждый webhook

**Экономия:** При 100 webhook событиях в день = **200-600 токенов/день**

**Важно:** Person ID и Organization ID особенно полезны, так как они используются при создании инвойсов и Checkout Sessions для получения email адресов и других данных контакта.

---

## ⚠️ Важные замечания

1. **Не все переменные доступны:** В зависимости от версии Pipedrive и типа автоматизации, некоторые переменные могут быть недоступны. Начните с минимального набора и добавляйте поля по мере возможности.

2. **Ключи полей:** Кастомные поля имеют уникальные ключи. Убедитесь, что используете правильный ключ для поля `invoice_type`.

3. **Previous values:** Переменная `{{deal.previous_stage_id}}` может быть недоступна в некоторых версиях Pipedrive. Если недоступна, оставьте поле пустым - система все равно будет работать.

4. **Тестирование:** После настройки протестируйте webhook, изменив стадию сделки и проверив логи сервера.

---

## 🧪 Тестирование

После настройки дополнительных полей:

1. Измените стадию сделки на "First Payment"
2. Проверьте логи сервера - должны появиться данные из webhook
3. Убедитесь, что система не делает лишних запросов к Pipedrive API

---

---

## 📋 Поля, извлекаемые из webhook и передаваемые в обработчики

Система автоматически извлекает следующие поля из webhook и передает их в обработчики (`processDealInvoiceByWebhook`, `createCheckoutSessionForDeal`, `processDealDeletionByWebhook`):

### Основные поля сделки:
- **id** - ID сделки (Deal ID, Deal_id, dealId, deal_id)
- **title** - Название сделки (Deal title, Deal_title, deal_title, title, Deal name, Deal_name, deal_name, name)
- **stage_id** - ID стадии (Deal_stage_id, Deal stage id, deal_stage_id, stage_id)
- **stage_name** - Название стадии (Deal stage, Deal_stage, deal_stage, stage_name)
- **status** - Статус сделки (Deal status, Deal_status, deal_status, status)

### Поля инвойса:
- **invoice_type** - Тип инвойса (Invoice type, Invoice, invoice_type, invoice, + поле по ключу INVOICE_TYPE_FIELD_KEY)
- **invoice_number** - Номер инвойса (Invoice number, Invoice_number, invoice_number, invoiceNumber, Invoice, + поле по ключу INVOICE_NUMBER_FIELD_KEY)

### Финансовые поля:
- **value** - Сумма сделки (Deal value, Deal_value, deal_value, value)
- **currency** - Валюта (Deal currency, Deal_currency, deal_currency, currency, Currency)

### Даты:
- **expected_close_date** - Ожидаемая дата закрытия (Expected close date, Deal_close_date, expected_close_date, expectedCloseDate)
- **close_date** - Дата закрытия (Deal_close_date, Deal closed date, close_date)

### Связи:
- **person_id** - ID персоны (Person ID, Contact id, Contact_id, person_id, personId, + поддержка объекта с .value)
- **organization_id** - ID организации (Organization ID, Organisation_id, organization_id, organizationId, + поддержка объекта с .value)
- **org_id** - Дубликат organization_id для совместимости

### Дополнительные поля:
- **lost_reason** - Причина потери сделки (Deal_lost_reason, Deal lost reason, lost_reason, lostReason)

### Все остальные поля:
Все поля из webhook, которые не перечислены выше, автоматически копируются в `currentDeal` через spread оператор и передаются в обработчики. Это гарантирует, что любые дополнительные поля из webhook будут доступны в обработчиках для запросов к CRM.

**Важно:** Все эти поля передаются в:
- `processDealInvoiceByWebhook(dealId, currentDeal)` - для создания проформ
- `createCheckoutSessionForDeal(dealWithWebhookData, ...)` - для создания Stripe платежей (данные мержатся с данными из API)
- `processDealDeletionByWebhook(dealId, currentDeal)` - для удаления проформ

**Дата создания:** 2025-11-18  
**Последнее обновление:** 2025-11-19


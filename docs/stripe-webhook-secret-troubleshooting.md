# 🔧 Устранение проблем с верификацией подписи Stripe webhook

## Проблема

Webhook'и от Stripe не проходят верификацию подписи:
```
Stripe webhook signature verification failed
No signatures found matching the expected signature for payload
```

## Причины

1. **Неправильный `STRIPE_WEBHOOK_SECRET`** - секрет не соответствует endpoint'у в Stripe Dashboard
2. **Несоответствие режима** - используется секрет от test mode для live mode или наоборот
3. **Изменение body прокси** - Render/Cloudflare могут изменять тело запроса (редко)

## Решение

### Шаг 1: Проверьте режим в Stripe Dashboard

1. Откройте [Stripe Dashboard](https://dashboard.stripe.com)
2. **Убедитесь, что вы в правильном режиме:**
   - Для продакшена: **Live mode** (синий переключатель в правом верхнем углу)
   - Для тестирования: **Test mode** (серый переключатель)

### Шаг 2: Проверьте endpoint URL

1. Перейдите в **Developers** → **Webhooks**
2. Найдите endpoint с URL: `https://invoices.comoon.io/api/webhooks/stripe`
3. **Убедитесь, что URL точно совпадает** (включая `https://`, без слеша в конце)

### Шаг 3: Получите правильный Signing Secret

1. Откройте ваш webhook endpoint в Stripe Dashboard
2. В разделе **"Signing secret"** нажмите **"Reveal"**
3. Скопируйте секрет (начинается с `whsec_...`)

**ВАЖНО:** 
- Для **Live mode** секрет начинается с `whsec_` или `whsec_live_`
- Для **Test mode** секрет начинается с `whsec_test_`
- Каждый endpoint имеет **уникальный** signing secret

### Шаг 4: Обновите `STRIPE_WEBHOOK_SECRET` в Render

1. Откройте Render Dashboard → ваш сервис
2. Перейдите в **Environment** → **Environment Variables**
3. Найдите `STRIPE_WEBHOOK_SECRET`
4. **Замените** на секрет из шага 3
5. **Сохраните** изменения
6. **Перезапустите** сервис (Render сделает это автоматически)

### Шаг 5: Проверьте логи

После обновления секрета проверьте логи:

```bash
node scripts/fetch-render-logs.js --lines=100 | grep -i "stripe webhook"
```

Должны появиться успешные сообщения:
```
✅ Stripe webhook получен | Тип: checkout.session.completed
```

## Диагностика

### Проверка формата секрета

Секрет должен:
- Начинаться с `whsec_`, `whsec_test_`, или `whsec_live_`
- Иметь длину около 38-40 символов
- Не содержать пробелов или переносов строк

### Проверка соответствия режима

В логах проверьте:
- `webhookSecretPrefix: "whsec_3C20..."` - должен соответствовать режиму
- Если используется `whsec_test_...` для live mode - это ошибка

### Проверка timestamp подписи

В логах проверьте:
- `signatureTimestamp` - должен быть не старше 5 минут
- Если timestamp слишком старый - Stripe отклонит подпись

## Частые ошибки

### ❌ Ошибка 1: Использование секрета от другого endpoint'а

**Симптом:** Верификация не проходит, хотя секрет правильного формата

**Решение:** Убедитесь, что используете секрет именно от endpoint'а `https://invoices.comoon.io/api/webhooks/stripe`

### ❌ Ошибка 2: Смешивание test и live режимов

**Симптом:** Секрет от test mode используется для live mode или наоборот

**Решение:** Используйте секрет, соответствующий текущему режиму в Stripe Dashboard

### ❌ Ошибка 3: Секрет с лишними пробелами

**Симптом:** Верификация не проходит

**Решение:** Убедитесь, что в `STRIPE_WEBHOOK_SECRET` нет пробелов в начале или конце

## Тестирование

После обновления секрета:

1. **Создайте тестовый платеж** в Stripe (минимальная сумма)
2. **Проверьте логи** - webhook должен пройти верификацию
3. **Проверьте Stripe Dashboard** → Webhooks → Recent events - должны быть успешные доставки (зеленые галочки)

## Дополнительная помощь

Если проблема сохраняется:

1. Проверьте логи с детальной диагностикой:
   ```bash
   node scripts/fetch-render-logs.js --lines=500 | grep -i "signature\|webhook"
   ```

2. Проверьте, что endpoint URL в Stripe Dashboard точно совпадает с вашим URL

3. Убедитесь, что вы используете правильный режим (Live/Test) в Stripe Dashboard

4. Попробуйте создать новый endpoint в Stripe Dashboard и использовать его секрет


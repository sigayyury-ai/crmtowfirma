# Руководство для Sales менеджеров: Работа с системой выставления инвойсов

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Как работает автоматизация](#как-работает-автоматизация)
3. [Обязательные поля в Pipedrive](#обязательные-поля-в-pipedrive)
4. [Процесс выставления инвойса](#процесс-выставления-инвойса)
5. [Графики платежей](#графики-платежей)
6. [Типичные ошибки и их решение](#типичные-ошибки-и-их-решение)
7. [Часто задаваемые вопросы](#часто-задаваемые-вопросы)

---

## Обзор системы

Система автоматически создает проформы (proforma invoices) в wFirma на основе данных из Pipedrive CRM. После создания проформы система автоматически:

- ✅ Создает проформу в wFirma
- ✅ Отправляет проформу клиенту по email
- ✅ Отправляет уведомление в Telegram (если настроено)
- ✅ Создает задачи для проверки платежей в Pipedrive
- ✅ Обновляет статус сделки в Pipedrive

**Важно:** Система работает автоматически, но требует правильного заполнения данных в Pipedrive.

---

## Как работает автоматизация

### Процесс создания инвойса

1. **Триггер:** Когда в сделке Pipedrive устанавливается тип инвойса "Proforma"
2. **Проверка данных:** Система проверяет наличие обязательных полей
3. **Создание контрагента:** Если контрагент не найден в wFirma, он создается автоматически
4. **Создание проформы:** Проформа создается в wFirma с данными из сделки
5. **Отправка email:** Проформа отправляется клиенту с настраиваемым текстом
6. **Telegram уведомление:** Если у клиента есть SendPulse ID, отправляется уведомление
7. **Создание задач:** Автоматически создаются задачи для проверки платежей
8. **Обновление статуса:** Статус инвойса в Pipedrive меняется на "Done"

### Что происходит автоматически

- ✅ Создание контрагента в wFirma (если его нет)
- ✅ Расчет суммы и валюты из сделки
- ✅ Определение графика платежей (50/50 или 100%)
- ✅ Создание задач для проверки платежей
- ✅ Отправка email клиенту
- ✅ Отправка Telegram уведомления (если настроено)

---

## Обязательные поля в Pipedrive

### Для сделки (Deal)

| Поле | Ключ поля | Обязательность | Описание |
|------|-----------|----------------|----------|
| **Название сделки** | `title` | ✅ Обязательно | Название сделки, отображается в проформе |
| **Сумма** | `value` | ✅ Обязательно | Сумма сделки в валюте сделки |
| **Валюта** | `currency` | ✅ Обязательно | Валюта сделки (PLN, EUR, USD) |
| **Тип инвойса** | `ad67729ecfe0345287b71a3b00910e8ba5b3b496` | ✅ Обязательно | Должен быть установлен "Proforma" (70) для запуска процесса |
| **Продукт** | `products` | ✅ Обязательно | Первый продукт в сделке используется для проформы |
| **Expected Close Date** | `expected_close_date` | ⚠️ Рекомендуется | Дата закрытия сделки, используется для расчета графика платежей |

### Для персоны (Person)

| Поле | Ключ поля | Обязательность | Описание |
|------|-----------|----------------|----------|
| **Email** | `email` или `primary_email` | ✅ Обязательно | Email для отправки проформы |
| **Имя** | `name` | ✅ Обязательно | Имя клиента |
| **Адрес** | `postal_address` | ⚠️ Рекомендуется | Адрес для проформы |
| **Город** | `postal_address_locality` | ⚠️ Рекомендуется | Город для проформы |
| **Почтовый индекс** | `postal_address_postal_code` | ⚠️ Рекомендуется | Почтовый индекс |
| **Страна** | `postal_address_country` | ⚠️ Рекомендуется | Страна (код ISO или название) |
| **SendPulse ID** | `ff1aa263ac9f0e54e2ae7bec6d7215d027bf1b8c` | ⚪ Опционально | ID для отправки Telegram уведомлений |

### Для продукта (Product)

| Поле | Обязательность | Описание |
|------|----------------|----------|
| **Название** | ✅ Обязательно | Название продукта, отображается в проформе |
| **Цена** | ✅ Обязательно | Цена продукта |
| **Валюта** | ✅ Обязательно | Валюта продукта (должна совпадать с валютой сделки) |

---

## Процесс выставления инвойса

### Пошаговая инструкция

#### Шаг 1: Создание/выбор сделки

1. Откройте сделку в Pipedrive или создайте новую
2. Убедитесь, что у сделки есть связанная персона (Person)
3. Добавьте продукт в сделку (если еще не добавлен)

#### Шаг 2: Заполнение обязательных полей

**В сделке:**
- ✅ Название сделки (`title`)
- ✅ Сумма (`value`) - должна совпадать с суммой продукта
- ✅ Валюта (`currency`) - PLN, EUR или USD
- ✅ Продукт (`products`) - первый продукт будет использован

**В персоне:**
- ✅ Email (`email` или `primary_email`)
- ✅ Имя (`name`)
- ⚠️ Адресные данные (для корректного отображения в проформе)

**В продукте:**
- ✅ Название продукта
- ✅ Цена продукта
- ✅ Валюта продукта

#### Шаг 3: Установка Expected Close Date (опционально)

**Важно:** Если `expected_close_date` установлена более чем на 2 месяца от текущей даты, система автоматически создаст график платежей 50/50:
- 50% предоплата - через 3 дня после создания проформы
- 50% остаток - за 1 месяц до expected_close_date

Если `expected_close_date` не установлена или меньше 2 месяцев, создается график 100% (полная оплата).

#### Шаг 4: Запуск процесса создания проформы

1. Найдите поле **"Тип инвойса"** (Invoice Type) в сделке
2. Установите значение **"Proforma"** (70)
3. Сохраните сделку

**После установки "Proforma":**
- ✅ Система автоматически начнет процесс создания проформы
- ✅ Статус инвойса автоматически изменится на "Done" после успешного создания
- ✅ Не нужно устанавливать "Done" вручную - система сделает это автоматически

#### Шаг 5: Проверка результатов

После успешного создания проформы:

1. **Проверьте email клиента** - проформа должна быть отправлена автоматически
2. **Проверьте задачи в сделке** - должны быть созданы задачи для проверки платежей:
   - "Проверка предоплаты" (для графика 50/50)
   - "Проверка остатка" (для графика 50/50)
   - "Проверка платежа" (для графика 100%)
3. **Проверьте статус инвойса** - должен быть "Done"

---

## Графики платежей

### График 50/50 (два платежа)

**Условия:**
- `expected_close_date` установлена и разница между сегодняшней датой и `expected_close_date` >= 30 дней (месяц)

**Как работает:**
- **Первый платеж (50%):** Предоплата 50% от суммы, срок оплаты - через 3 дня после создания проформы
- **Второй платеж (50%):** Остаток 50% от суммы, срок оплаты - за 1 месяц до `expected_close_date`

**Пример:**
- Сумма: 1000 PLN
- Expected close date: 2026-02-01 (через 3 месяца)
- Первый платеж: 500 PLN до 2025-11-07 (issueDate + 3 дня)
- Второй платеж: 500 PLN до 2026-01-01 (expected_close_date - 1 месяц)

**Создаются задачи:**
1. "Проверка предоплаты" - срок: первый платеж + 3 дня (2025-11-10)
2. "Проверка остатка" - срок: дата второго платежа (2026-01-01)

### График 100% (один платеж)

**Условия:**
- `expected_close_date` не установлена ИЛИ
- Разница между сегодняшней датой и `expected_close_date` < 30 дней (меньше месяца)

**Как работает:**
- **Полная оплата:** 100% от суммы, срок оплаты - через 3 дня после создания проформы

**Пример:**
- Сумма: 1000 PLN
- Expected close date: не установлена
- Платеж: 1000 PLN до 2025-11-07 (issueDate + 3 дня)

**Создается задача:**
1. "Проверка платежа" - срок: дата платежа (2025-11-07)

---

## Типичные ошибки и их решение

### Где выводятся ошибки?

Ошибки системы логируются в следующие места:

1. **Файлы логов:**
   - `logs/error.log` - только ошибки (error level)
   - `logs/combined.log` - все логи (info, warn, error)
   - Файлы автоматически ротируются при достижении 5MB

2. **Консоль сервера:**
   - В режиме разработки ошибки выводятся в консоль
   - В production режиме ошибки только в файлы логов

3. **Frontend интерфейс:**
   - Если есть доступ к админ-панели, ошибки отображаются в интерфейсе
   - URL: `https://invoices.comoon.io` (требуется авторизация)

4. **Pipedrive:**
   - Критические ошибки могут отображаться в логах Pipedrive
   - Проверьте статус сделки и связанные заметки

**Как посмотреть логи:**
- На сервере: `tail -f logs/error.log` или `tail -f logs/combined.log`
- В Render.com: перейдите в раздел "Logs" в настройках сервиса
- В админ-панели: проверьте раздел "System Status"

---

### ❌ Ошибка: "No invoice type specified in deal"

**Причина:** Поле "Тип инвойса" не установлено или имеет неправильное значение.

**Решение:**
1. Убедитесь, что поле "Тип инвойса" установлено на "Proforma" (70)
2. Не устанавливайте "Done" (73) вручную - система сделает это автоматически

### ❌ Ошибка: "No product found in deal"

**Причина:** В сделке нет продуктов или продукт не связан правильно.

**Решение:**
1. Добавьте продукт в сделку
2. Убедитесь, что продукт имеет название и цену
3. Убедитесь, что валюта продукта совпадает с валютой сделки

### ❌ Ошибка: "No email found for person"

**Причина:** У персоны нет email адреса.

**Решение:**
1. Добавьте email в персону (Person)
2. Убедитесь, что email указан в поле `email` или `primary_email`

### ❌ Ошибка: "Failed to create contractor"

**Причина:** Недостаточно данных для создания контрагента в wFirma.

**Решение:**
1. Заполните адресные данные персоны:
   - Адрес (`postal_address`)
   - Город (`postal_address_locality`)
   - Почтовый индекс (`postal_address_postal_code`)
   - Страна (`postal_address_country`)

### ❌ Проформа не отправляется по email

**Причина:** 
- Email клиента не указан или указан неправильно
- Проблемы с wFirma API

**Решение:**
1. Проверьте email в персоне
2. Убедитесь, что email указан в поле `email` или `primary_email`
3. Проверьте логи системы для детальной информации:
   - Файл: `logs/error.log` или `logs/combined.log`
   - Ищите строки с "Failed to send invoice by email" или "Email sending failed"

### ❌ Создается только 1 задача вместо 2

**Причина:** 
- `expected_close_date` не установлена или установлена менее чем на 30 дней (месяц)
- Разница между сегодняшней датой и `expected_close_date` < 30 дней

**Решение:**
1. Установите `expected_close_date` так, чтобы разница между сегодняшней датой и `expected_close_date` была >= 30 дней (месяц)
2. Проверьте логи системы:
   - Файл: `logs/combined.log`
   - Ищите строки с "Payment schedule calculation" или "Determining task count"

---

## Часто задаваемые вопросы

### Q: Нужно ли вручную устанавливать "Done" после создания проформы?

**A:** Нет, система автоматически устанавливает "Done" после успешного создания проформы в wFirma. Не нужно делать это вручную.

### Q: Что делать, если проформа не создалась?

**A:** 
1. Проверьте логи системы для детальной информации об ошибке
2. Убедитесь, что все обязательные поля заполнены
3. Проверьте, что тип инвойса установлен на "Proforma" (70)
4. Свяжитесь с технической поддержкой, если проблема сохраняется

### Q: Как изменить сумму или данные проформы после создания?

**A:** После создания проформы в wFirma изменения нужно вносить вручную в wFirma. Система не изменяет уже созданные проформы.

### Q: Можно ли создать проформу повторно для той же сделки?

**A:** Технически можно, но это создаст дубликат проформы. Рекомендуется создавать новую сделку для нового инвойса.

### Q: Как работает отправка Telegram уведомлений?

**A:** 
- Telegram уведомления отправляются только если у персоны заполнено поле "SendPulse ID"
- Если SendPulse ID не указан, проформа все равно создается и отправляется по email
- Уведомление отправляется автоматически после создания проформы

### Q: Что происходит, если у клиента нет адресных данных?

**A:** 
- Система попытается создать контрагента с минимальными данными
- Для иностранных клиентов (не PL) система использует дефолтные значения
- Рекомендуется заполнить адресные данные для корректного отображения в проформе

### Q: Как понять, какой график платежей будет использован?

**A:** 
- Если `expected_close_date` установлена более чем на 2 месяца от текущей даты → график 50/50
- Если `expected_close_date` не установлена или менее 2 месяцев → график 100%
- После создания проформы проверьте описание в проформе - там указан график платежей

### Q: Можно ли изменить текст email перед отправкой?

**A:** Нет, текст email настроен в системе и одинаков для всех проформ. Текст включает:
- Приветствие
- Номер инвойса
- Инструкции по оплате

---

## Полезные ссылки

- **Pipedrive CRM:** https://app.pipedrive.com
- **wFirma:** https://wfirma.pl
- **Система инвойсов:** https://invoices.comoon.io

---

## Контакты поддержки

При возникновении проблем или вопросов обращайтесь к технической поддержке.

---

**Последнее обновление:** 2025-11-04  
**Версия документа:** 1.0


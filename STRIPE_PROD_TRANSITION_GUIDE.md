# 🚀 Инструкция по переходу с тестового режима Stripe на продакшн

## 📋 Шаг 1: Получение LIVE ключей Stripe

### В Stripe Dashboard:
1. Откройте [Stripe Dashboard](https://dashboard.stripe.com)
2. **Переключитесь в Live mode** (синий переключатель в правом верхнем углу)
3. Перейдите в раздел **Developers** → **API keys**
4. Скопируйте:
   - **Secret key** (начинается с `sk_live_...`) - основной ключ
   - **Publishable key** (начинается с `pk_live_...`) - для фронтенда

## 📋 Шаг 2: Обновление переменных окружения

### Измените в файле `.env`:

**Настройка:**
```bash
STRIPE_API_KEY=sk_live_ваш_live_секретный_ключ
```

### Важно:
- `STRIPE_MODE` должен быть `live` (не `production`)
- Ключ должен начинаться с `sk_live_`

## 📋 Шаг 3: Настройка продакшен вебхуков

### В Stripe Dashboard:
1. Убедитесь, что находитесь в **Live mode**
2. Перейдите в **Developers** → **Webhooks**
3. Нажмите **"Add endpoint"**
4. **Endpoint URL:** `https://invoices.comoon.io/api/webhooks/stripe`
5. **Description:** `Обработка платежей для CRM - продакшн`

### Выберите события:
- ✅ `checkout.session.completed`
- ✅ `checkout.session.async_payment_succeeded`
- ✅ `checkout.session.async_payment_failed`
- ✅ `checkout.session.expired`
- ✅ `payment_intent.succeeded`
- ✅ `payment_intent.payment_failed`
- ✅ `charge.refunded`

### После создания:
1. Откройте созданный webhook
2. Нажмите **"Reveal"** на Signing secret
3. Скопируйте секрет (начинается с `whsec_...`)

## 📋 Шаг 4: Обновление webhook секрета

### В файле `.env` замените:
```bash
# Старый тестовый секрет
STRIPE_WEBHOOK_SECRET=whsec_test_старый_секрет

# Новый продакшен секрет
STRIPE_WEBHOOK_SECRET=whsec_live_новый_секрет
```

## 📋 Шаг 5: Перезапуск сервера

```bash
# Если используете PM2
pm2 restart all

# Если запускаете напрямую
npm start

# Или на Render - перезапустите сервис через dashboard
```

## 🔍 Проверка перехода

### 1. Проверьте логи сервера:
```bash
tail -f logs/combined.log | grep -i stripe
```

### 2. В Stripe Dashboard:
- Перейдите в **Webhooks** → ваш endpoint
- Проверьте вкладку **"Recent events"**
- Должны быть успешные доставки

### 3. Тестовый платеж:
⚠️ **Создайте платеж на минимальную сумму (например, 1 PLN)**
- Проверьте, что webhook сработал
- Убедитесь, что статус платежа обновился в базе данных

## ⚠️ Важные предупреждения

### ❌ Чего НЕ делать:
- Не тестируйте на больших суммах
- Не используйте реальные данные клиентов для тестирования
- Не удаляйте тестовые ключи до полной проверки

### ✅ Рекомендации:
- **Сохраните тестовые ключи** в комментариях или отдельном файле
- **Два аккаунта Stripe** - удобно для тестирования
- **Пошаговая проверка** - не меняйте все сразу

### 🔄 Откат при проблемах:
Если что-то пошло не так, вернитесь к тестовым ключам:
```bash
STRIPE_API_KEY=sk_live_ваш_live_ключ
STRIPE_WEBHOOK_SECRET=whsec_ваш_секрет
```

## 📞 Поддержка

При проблемах проверьте:
1. Правильность ключей (live ключи для live режима)
2. Доступность webhook URL из интернета
3. Логи сервера на наличие ошибок
4. Статус платежей в базе данных

## ✅ Финальная проверка

После успешного перехода:
- [ ] Live платежи обрабатываются корректно
- [ ] Webhook события приходят и обрабатываются
- [ ] Статусы платежей обновляются в CRM
- [ ] Нет ошибок в логах
- [ ] Тестовые платежи не влияют на продакшн данные

---

**🎉 Поздравляем! Вы успешно перешли на Stripe продакшн.**
